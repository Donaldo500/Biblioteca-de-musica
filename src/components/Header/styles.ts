import styled from 'styled-components';

const HeaderComponent = styled.header`
    padding: 20px;
    display: flex;
    justify-content: space-between;
`;

const Headerimg = styled.svg`
    max-width: 50px;
    width: 100%;
    height: 100%;
    cursor: pointer;
`

export{
    HeaderComponent,
    Headerimg
}